<%= form_for(@type_repair) do |f| %>
  <% if @type_repair.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_repair.errors.count, "error") %> prohibited this type_repair from being saved:</h2>

      <ul>
      <% @type_repair.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
